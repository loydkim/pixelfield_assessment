name: pixelfield_project
description: "A new Pixelfield project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  flutter_bloc: ^9.1.0
  equatable: ^2.0.7

  # Database
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  hive_generator: ^2.0.0

  # Utilities
  connectivity_plus: ^6.1.3
  build_runner: ^2.4.15

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  hive_test: ^1.0.0
  bloc_test: ^10.0.0
flutter:
  uses-material-design: true

  assets:
    - assets/
    - jsons/